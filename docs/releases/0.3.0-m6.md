# 0.3.0-m6 verification record

Date: 2026-08-25 (Asia/Shanghai)

This record covers the host-aligned release candidate built from the M6 release tree. Remote CI
run links are tracked after the release commit is pushed; all evidence below was collected locally
or from the designated Android device before publication.

## Artifact identity

| Field | Verified value |
|---|---|
| Application ID | `io.github.supermonster003.autojs6.plugin.kotlin.runtime` |
| Version | `0.3.0-m6` (`VERSION_BUILD=3`) |
| Android SDK range | minSdk 26, targetSdk 36 |
| Release APK size | 45,339,694 bytes |
| Release APK SHA-256 | `894850db373e7f5af452cda65a5ca4387e7ab9f2a96cc95d5c070e15a5842bef` |
| Host/plugin signer SHA-256 | `31a681fcfffb3e428420cae280ded89292b12a3b0f59e19b7a73e32a8ae4c213` |

The signer digest was identical for the trusted AutoJs6 APK, `app-debug.apk`, and
`app-release.apk`. Signing material remained in the ignored `sign.properties` and `app/sm003.jks`
files and is not part of the source tree.

## Local release gates

The following commands completed with exit code 0 in offline mode:

```powershell
.\gradlew.bat -p build-logic/platform-versions test --offline --stacktrace
.\gradlew.bat :app:verifyPinnedInputs :app:verifyPinnedInputsFailurePath :app:verifyPinnedInputsWiring :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease :app:assembleDebugAndroidTest :app:lintDebug --offline --stacktrace
```

- Unit tests: 119 tests in 40 suites; 0 failures, 0 errors, 0 skipped.
- Frozen Protocol: valid-input, one-byte-corruption, and assemble-wiring gates passed.
- Assemblies: debug, host-aligned release, and debug Android-test APKs passed.
- Lint: no new issues; 14 historical findings remain explicitly filtered by the reviewed baseline.
  Three remote, time-varying dependency-version checks are disabled because versions are frozen and
  upgraded through reviewed build-logic changes.
- GitHub Actions workflow: `actionlint` passed locally.

## Device verification

| Field | Value |
|---|---|
| ADB serial | `QV710AF65F` |
| Device | Sony `XQ-AT72` |
| Android | 12 (API 31) |
| AutoJs6 host | `6.8.0`, version code 5276 |
| Installed plugin | `0.3.0-m6`, version code 3, release/non-debuggable |

`adb install -r` succeeded without uninstalling the host or losing plugin data. Package discovery
returned one Kotlin provider service for each required action:

- `org.autojs.plugin.INFO` → `JavaPluginInfoService`
- `org.autojs.plugin.JVM_SOURCE` → `JavaSourceCompilerService`

### Production host/provider path

`samples/m5-capabilities.kt` ran through AutoJs6 and exercised all four Protocol 1.1 capabilities:

- `APP_LAUNCH`: returned `true` and brought AutoJs6 to the foreground.
- `CONSOLE_STREAM`: emitted the expected start, stdout, stderr, and terminal markers.
- `SLEEP`: completed the 500 ms cancellable sleep.
- `TOAST`: the expected toast was observed.

The first end-to-end run completed in 7,107 ms and an immediate second run in 1,157 ms. This is
the production-path warm-cache signal; cache hit/miss authentication and telemetry semantics are
covered directly by unit tests because Protocol 1.1 intentionally exports no cache-specific result
field. After restoring the release APK from the instrumentation target, a final capability run also
completed successfully in 2.210 seconds.

### Active cancellation and worker retirement

While `samples/m6-cancellation.kt` was sleeping, AutoJs6 ran `samples/m6-host-cancel.js`, which
invokes the host's `engines.stopAll()` API. Evidence:

- `M6_CANCELLATION_STARTED` was emitted while a fresh `:worker` process was present.
- `M6_HOST_CANCEL_REQUESTED` was emitted by the host-side cancellation helper.
- AutoJs6 reported `[JVM_SOURCE_CANCELLED] JVM source execution was cancelled`.
- The disposable worker was present at 250 ms and absent by the 500 ms observation; it remained
  absent through 7 seconds, while the separate `:compiler` process stayed healthy.
- `M6_CANCELLATION_UNEXPECTED_COMPLETION` was never emitted.

The device's accessibility-service setting was restored byte-for-byte after an earlier volume-key
probe, and the final cancellation path did not require changing that setting.

### Android instrumentation

The debug target and test APK were installed temporarily and the following real-device test passed:

```text
KotlinProviderPipelineInstrumentedTest.kotlinD8DexValidationAndEntryInvocationRunOnAndroid
OK (1 test), 3.329 seconds
```

This covers on-device Kotlin compilation, D8 conversion, DEX validation/loading, and entry
invocation. The test package was then uninstalled and the host-aligned, non-debuggable release APK
was reinstalled and re-smoked.

## Private publication boundary

The GitHub repository is intentionally Private. Public APK publication and the AutoJs6 official
schema-v2 index update remain deferred; neither is required to close the private M6 engineering
milestone.
