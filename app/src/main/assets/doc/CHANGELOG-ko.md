******

### 릴리스 이력

******

# v0.7.1

###### 2026/09/13

* `수정` 플러그인 센터에서 보호된 진입점으로 새 설치를 활성화하고 설치된 패키지의 메타데이터를 표시
* `개선` 호스트 활성화, 메타데이터, 다국어 문서 및 서명된 APK 수집을 공통 규칙에 맞게 정리

# v0.7.0

###### 2026/09/11

* `개선` 빌드 시 의도하지 않은 네이티브 의존성을 거부하고 JSON 보고서 생성

# v0.7.0-m10

###### 2026/08/26

* `힌트` 공개 기능은 Protocol 1.1 / Entry API 2를 유지합니다. 1.2 신규 기능은 호스트에 구현되기 전까지 개방되지 않습니다
* `새 기능` JVM Source Protocol 1.2 기능 제안을 추가하고 호스트 검토에 제출: 제한된 클립보드, 사용자 승인 문서, 호스트 대리 HTTPS, 호스트 관리 알림
* `새 기능` 「인가 → payload 검증 → 디스패치 → 응답 검증」 4단계 호스트 호출 파이프라인 추가. 기존 `app.launch`와 `toast.show`는 동작 변화 없이 이관 완료
* `개선` 동결 프로토콜 AAR을 schema-2 출처 잠금으로 업그레이드하고 staging 전용 갱신 스크립트와 완전한 SOP 제공
* `개선` 신구 호스트/플러그인 조합, 다운그레이드, 안정적 거부 경로를 포괄하는 7가지 1.1/1.2 프로토콜 협상 테스트 추가

# v0.6.0-m9

###### 2026/08/26

* `힌트` `Dispatchers.Main`, 완전한 `kotlin-reflect`, kotlinx-serialization은 여전히 스크립트에서 사용 불가
* `새 기능` 정확히 고정된 `kotlinx-coroutines-core-jvm` 1.11.0을 스크립트 라이브러리에 추가. 구조화 동시성과 협조적 취소 지원
* `새 기능` 바로 실행 가능한 코루틴 예제 `samples/coroutines.kt` 추가
* `개선` 런타임 지문과 컴파일 캐시 키에 코루틴 라이브러리 식별 정보를 포함하여 업그레이드 후 이전 캐시 전체 자동 무효화

# v0.5.0-m8

###### 2026/08/25

* `새 기능` 흔한 4가지 컴파일 오류의 이중 언어 해설 샘플 추가: import 누락, 타입 불일치, 진입 인터페이스 미구현, 패키지 불일치
* `수정` Windows 경로 구분자 차이로 K2 행/열 위치가 사라지는 문제 수정
* `수정` 선두 UTF-8 BOM 제거 시 첫 행 위치가 밀리지 않도록 수정
* `개선` 패키지 정책 문구 명확화: 일반 ASCII 식별자만 지원하며, 거부 시 오해를 부르는 오류 대신 안정적이고 읽기 쉬운 메시지 표시
* `개선` 진단은 UTF-8 예산으로 관리되며 Unicode 코드 포인트 경계에서만 잘림 — 이모지가 반쪽 나는 일이 없습니다

# v0.4.0-m7

###### 2026/08/25

* `새 기능` 실기기 스트레스/장애 주입 하네스 (`m7-harness`)와 PowerShell/POSIX 원커맨드 기기 스크립트 추가
* `수정` 캐시 비활성화 시 컴파일러 상태가 호스트 바인딩 주기를 넘어 잔류하는 문제 수정
* `개선` 결정론적 class-only API 24 컴파일 클래스패스로 전환하여 release APK를 45.34 MB에서 30.38 MB로 축소 (-32.99%)
* `개선` 컴파일 캐시: 웜 히트 중앙값 46 ms 대 콜드 577 ms — 실기기 기준 약 12.5배 가속
* `개선` 컴파일러 프로세스는 바인딩 단위로 유지되고 최종 언바인드 시 폐기. 메타데이터 발견은 독립된 `:discovery` 프로세스로 이동

# v0.3.0-m6

###### 2026/08/25

* `새 기능` CHANGELOG, 릴리스 체크리스트, GitHub Actions 전체 CI 게이트 추가: 단위 테스트, 두 APK, Lint, 프로토콜 무결성 검사
* `개선` 빌드 로직을 build-logic으로 집중하고 platform-versions 플러그인 소스 스냅숏을 내장하여 깨끗한 체크아웃만으로 빌드 가능
* `개선` minSdk 26으로 불필요해진 Android API 분기 제거

# v0.2.0-m5

###### 2026/08/22

* `힌트` 첫 사용 가능한 마일스톤 버전 (M1–M5 포함). 동일 서명 호스트 검증, 기능별 인가, 일회용 worker 프로세스, fail-closed 검증을 처음부터 활성화
* `새 기능` 독립 Kotlin 소스 컴파일/실행 플러그인 성립: Kotlin/JVM 2.3.21과 D8 8.13.17 내장, 컴파일과 실행을 별도 프로세스로 분리
* `새 기능` Protocol 1.1 단일 파일 소스 프로필 구현: 패키지/진입 클래스 분석, 정제된 진단, 완전한 실패 단계 매핑
* `새 기능` 개별 인가되는 4가지 호스트 기능 구현: 앱 실행, 콘솔 스트림, sleep, toast
* `새 기능` 인증된 컴파일 캐시와 텔레메트리 구현
